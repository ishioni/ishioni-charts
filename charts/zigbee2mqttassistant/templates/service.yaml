apiVersion: v1
kind: Service
metadata:
  name: {{ include "zigbee2mqttassistant.fullname" . }}
  labels:
    {{- include "zigbee2mqttassistant.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zigbee2mqttassistant.selectorLabels" . | nindent 4 }}
