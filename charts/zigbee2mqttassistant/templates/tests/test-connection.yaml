apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "zigbee2mqttassistant.fullname" . }}-test-connection"
  labels:
    {{- include "zigbee2mqttassistant.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "zigbee2mqttassistant.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
